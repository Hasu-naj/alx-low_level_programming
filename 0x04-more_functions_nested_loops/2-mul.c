#include "main.h"
/**
 *mul - a function that multiplies two integers
 * @a: a is a parameter
 * @b: b is a parameter
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int m;

	m = a * b;
	return (s);
}
