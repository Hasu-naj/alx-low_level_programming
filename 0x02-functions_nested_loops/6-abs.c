#include "main.h"

/**
 *  _abs(int) -  the absolute value of an integer
 *
 *  Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
	return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}

