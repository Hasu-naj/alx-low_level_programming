#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @a: a is a parameter
 * @b: b is a parameter
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	int s;

	s = a + b;
	return (s);
}
